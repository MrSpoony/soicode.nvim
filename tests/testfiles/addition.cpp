#include <bits/stdc++.h>

using namespace std;

int main() {
  short a, b;
  cin >> a >> b;
  if (a == 420)
    while (1) {
    }
  else if (a == 69)
    exit(1);
  else if (a == 6969)
    assert(false);
  for (short i = 0; i < b; i++) {
    a++;
  }

  cout << a << endl;

  return 0;
}
