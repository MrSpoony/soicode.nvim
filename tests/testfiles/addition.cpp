#include <bits/stdc++.h>

using namespace std;

int main() {
  int a, b;
  cin >> a >> b;
  if (a == 420)
    while (1) {
    }
  else if (a == 69)
    exit(1);
  else if (a == 6969)
    assert(false);
  else if (a == 70) {
    cout << 111 << "\n";
    return 0;
  }

  cout << a + b << "\n";

  return 0;
}
