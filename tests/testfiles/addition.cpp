#include <bits/stdc++.h>

using namespace std;

int main() {
  ushort a, b;
  cin >> a >> b;
  if (a == 420)
    while (1) {
    }
  else if (a == 69)
    exit(1);
  else if (a == 6969)
    assert(false);
  for (ushort i = 0; i < b; i++) {
    a++;
  }

  cout << a << endl;

  return 0;
}
